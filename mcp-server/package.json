{
  "name": "devenv-mcp",
  "version": "1.0.0",
  "description": "MCP server that exposes every globally-installed tool from the local ai-dev-envbuild inventory as a discrete MCP tool",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0"
  }
}
